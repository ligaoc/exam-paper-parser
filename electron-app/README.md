# 钻石画设计器 - Electron 桌面版

将图片转换为钻石画图案的桌面应用程序。

## 功能特性

- 📷 支持 PNG、JPEG 图片上传
- 🎨 自动颜色匹配到钻石颜色卡
- 📊 颜色用量统计
- 🖼️ 实时预览生成效果
- 💾 导出高清 PNG 图片

## 开发

### 环境要求

- Node.js 18+
- npm 或 yarn

### 安装依赖

```bash
cd electron-app
npm install
```

### 开发模式

```bash
npm run electron:dev
```

### 构建应用

```bash
# 构建 Windows 安装包
npm run dist

# 仅构建，不打包
npm run pack
```

## 项目结构

```
electron-app/
├── src/
│   ├── main/           # Electron 主进程
│   │   ├── main.js     # 主进程入口
│   │   ├── preload.js  # 预加载脚本
│   │   ├── ipcHandlers.js
│   │   ├── services/   # 业务逻辑
│   │   └── data/       # 颜色卡数据
│   └── renderer/       # Vue 渲染进程
│       ├── App.vue
│       ├── components/
│       └── api/
├── build/              # 打包资源（图标等）
├── release/            # 打包输出目录
└── tests/              # 测试文件
```

## 技术栈

- Electron 28
- Vue 3
- Vite 5
- Sharp (图像处理)
- Element Plus (UI 组件)
